services:
  - type: web
    name: wordcount
    runtime: native
    buildCommand: g++ -std=c++17 -O2 -o wordcount WordCount.cpp
    startCommand: ./wordcount
    envVars:
      - key: PORT
        value: 10000
